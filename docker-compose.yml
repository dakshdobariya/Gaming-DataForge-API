services:
  db:
    image: postgres:16-alpine
    environment:
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: postgres
      POSTGRES_DB: gaming
    ports:
      - "5432:5432"
    volumes:
      - gaming_pgdata:/var/lib/postgresql/data

volumes:
  gaming_pgdata:
